## Summary

<!-- Briefly describe your changes and the motivation behind them -->

## Changes

-

## Test Plan

- [ ] `npm test` passes (runs `tsc` build + `node --test test/*.test.mjs`)
- [ ] Added/updated tests for new behavior (if applicable)
- [ ] Verified Unicode/edge-case inputs (homoglyphs, combining marks, RTL) where relevant

## Checklist

- [ ] TypeScript builds cleanly (`npm run build`)
- [ ] Public API changes reflected in `dist/index.d.ts` exports
- [ ] README updated (if user-facing behavior changed)
- [ ] No breaking changes — or breaking changes are called out above
